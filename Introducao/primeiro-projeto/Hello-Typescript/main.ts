let MinhaVarial = "minha variável";

function MayFunc (x, y)
{
    return x + y;
}

let Numeros = [1,2,3];

Numeros.map(function (valor) 
{
    return valor * 2;
});

Numeros.map(valor => valor * 2);